<Window x:Class="WeChatJevHud.Overlay.HudOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WeChatJevHud.Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="Manual"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        TextOptions.TextFormattingMode="Ideal">

  <Border Background="#F2232326" CornerRadius="10" Padding="14,12,14,12"
          BorderBrush="#33FFFFFF" BorderThickness="1">
    <StackPanel>
      <TextBlock Text="Jev" FontSize="10" Foreground="#8C8C94" Margin="0,0,0,6" />

      <TextBlock x:Name="MessageText" FontSize="13" FontWeight="SemiBold" Foreground="#F2F2F4"
                 TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="40" />

      <TextBlock x:Name="HeadlineText" FontSize="12" Foreground="#E8B84B" Margin="0,8,0,0"
                 TextWrapping="Wrap" />

      <TextBlock x:Name="AdviceText" FontSize="13" FontWeight="SemiBold" Foreground="#5FBF8F"
                 Margin="0,4,0,0" TextWrapping="Wrap" />

      <ItemsControl x:Name="RowList" Margin="0,8,0,0">
        <ItemsControl.ItemTemplate>
          <DataTemplate DataType="{x:Type local:OverlayRow}">
            <Grid Margin="0,2,0,0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="{Binding Label}" FontSize="12" Foreground="#C6C6CC"
                         TextTrimming="CharacterEllipsis" />
              <TextBlock Grid.Column="1" Text="{Binding ProbabilityText}" FontSize="12"
                         Foreground="#8C8C94" Margin="10,0,0,0" />
            </Grid>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </Border>
</Window>
