<Window x:Class="Yanwai.Overlay.HudOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Yanwai.Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="Manual"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        TextOptions.TextFormattingMode="Ideal">

  <!-- A grey note in WeChat's dark palette: close to the bubbles, clearly not one of them. -->
  <Border x:Name="Root" Background="#F2333335" CornerRadius="4" Padding="12,8,12,9"
          VerticalAlignment="Top">
    <StackPanel>
      <TextBlock Text="Jev：" FontSize="12" Foreground="#9A9AA0" />
      <ItemsControl x:Name="SectionList">
        <ItemsControl.ItemTemplate>
          <DataTemplate DataType="{x:Type local:OverlaySection}">
            <StackPanel Margin="0,4,0,0">
              <TextBlock Text="{Binding Heading}" FontSize="12" Foreground="#D6D6DA" TextWrapping="Wrap" />
              <ItemsControl ItemsSource="{Binding Rows}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate DataType="{x:Type local:OverlayRow}">
                    <TextBlock Text="{Binding Line}" FontSize="12" Foreground="#B4B4BA"
                               Margin="2,1,0,0" TextTrimming="CharacterEllipsis" />
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </Border>
</Window>
